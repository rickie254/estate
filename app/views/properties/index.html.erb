<div class="row justify-content-center">
  <div class="col-lg-10">

    <div class="card mb-2">
      <div class="card-header">
        <div class="row">
          <div class="col-sm-7">
            <h2>
              <%= @breadcrumbs %>
            </h2>
          </div>
          <div class="col-sm-5 text-right">
            <h2>
              <%= pluralize @properties.total_count, "imóvel", "imóveis" %>
            </h2>
          </div>
        </div>
      </div>
    </div>

    <% if @properties.length > 0 %>
      <% @properties.each do |property| %>
        <%= render "partials/property_summary", property: property %>
      <% end %>
    <% end %>

  </div>
</div>

<div class="row justify-content-center">
  <div class="col-auto">
    <%= paginate @properties %>
  </div>
</div>
